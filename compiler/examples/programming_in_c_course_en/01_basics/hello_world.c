/* Include the input/output library. */
#include <stdio.h>

int main(void) {
    /* Print a greeting message to the screen. */
    printf("Hello World!\n");
    return 0;  /* Return 0 to indicate the program finished without problems. */
}
